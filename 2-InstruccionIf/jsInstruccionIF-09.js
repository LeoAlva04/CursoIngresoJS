function mostrar()
{
	let num
	//Math.round(Math.random(num)*num: se utiliza para generar un numero cualquiera entre 2 numeros 
	
	num = Math.round(Math.random(1)*10);

	alert("Número aleatorio entre 1 y 10:"+num);


}
